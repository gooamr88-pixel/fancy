<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG VARIANT ONLY. This file shadows the main one for debug builds.

  Cleartext is permitted to the emulator loopback alias and the RFC1918 ranges a
  dev laptop lives on, so the app can talk to a local backend. It is scoped to
  named hosts rather than a blanket cleartextTrafficPermitted="true".

  Release builds never see this file — verify with:
    ./gradlew :app:assembleRelease
    unzip -p app/build/outputs/apk/release/app-release.apk AndroidManifest.xml | strings | grep -i cleartext
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
